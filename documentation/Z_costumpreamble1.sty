\ProvidesPackage{Z_costumpreamble1}

\usepackage{fullpage}
\usepackage[left=1in,top=1in,right=1in,bottom=1in,headheight=3ex,headsep=3ex]{geometry}
\usepackage{graphicx}
\usepackage{float}
\usepackage{relsize}
\usepackage{csquotes}
\usepackage{lmodern} % better font varieties

\newcommand{\blankline}{\quad\pagebreak[2]}


% COSTUM COMMANDS
% footnotes
\newcommand{\fn}[1]{\footnote{\doublespacing \hangpara{2em}{1} #1}} % makes a new footnote command \fn{} with a hanging indent (hanging indent starts after the first line)
\newcommand{\costthank}[1]{\fnsymbol{\hangpara{3em}{1} #1}} % makes a new footnote command \costthank{} with a hanging indent (hanging indent starts after the first line)

% itemization
%\newcommand{\costitem}{\item[o]} % costum item
\newcommand{\costitem}{\item[--]} % costum item

\newcommand{\costsubsection}[1]{\subsection*{\relsize{1}{#1}}}
\newcommand{\costsubsubsection}[1]{\subsubsection*{\-\hspace{-.025cm} #1}}


\usepackage[backend=biber,
			style=authoryear,
			doi=false,
			isbn=false,
			url=false,
			eprint=false,
			maxnames=2,%										% 2020-06-22 % include max. 2 names; more are truncated to '<minnames> et al.' before 'et al.'
			minnames=1,%										% 2020-06-22 % more than <maxnames> lists are truncated to '<minnames> et al.' before 'et al.'			
			maxbibnames=99,%									% 2020-06-22 % include all authors in bibliography
			useprefix=true,%									% 2020-06-22 % include 'de' 'da' in surname
			dashed=false,%										% 2020-06-22 % reprint recurring names in bilbiography
			uniquename=false,%									% 2020-06-22 % don't include first name initial if authors appear multiple times
			uniquelist=false%									% 2020-06-22 % don't make 'et al.' lists unique			
			]{biblatex}  % 2020-06-09 % reference management NEW
			
			